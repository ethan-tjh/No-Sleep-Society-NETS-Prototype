// Mock user identity for the single-user prototype.
// In production this would come from NETS' authenticated session/token —
// the field name (userToken) is what matters for now, not the value.

export const currentUserToken = 'user_demo_001';